```
## ファイル変更履歴

| ファイル | 直近の変更 | 変更規模 | 最終更新者 |
|---------|-----------|---------|-----------|
| {ファイルパス} | {コミットメッセージ要約} | {+N/-M 行} | {著者} |

## ホットスポット（直近3ヶ月）

| ファイル | 変更回数 | 変更者数 | 並行開発リスク |
|---------|---------|---------|-------------|
| {ファイルパス} | {N}回 | {M}人 | {低/中/高} |

## 大規模変更

| 日時 | コミット | 変更規模 | 概要 |
|------|---------|---------|------|
| {YYYY-MM-DD} | {short hash} | {+N/-M 行} | {変更概要} |

## ブランチ状態

- 現在のブランチ: {ブランチ名}
- ベースからの差分コミット: {N}件
- 未コミット変更: {有/無}（{ファイル数}ファイル）

## 並行開発リスク

- {リスクの要約（同じファイルを複数人が変更している場合等）}
```

**並行開発リスクの基準**:
- **高**: 直近3ヶ月で3人以上が同一ファイルを変更、かつ変更回数が10回以上
- **中**: 直近3ヶ月で2人以上が同一ファイルを変更、かつ変更回数が5回以上
- **低**: 上記に該当しない

**報告ルール**:
- プロンプトで依頼された調査パターンのセクションのみ出力する（依頼されていないセクションは省略）
- 各セクションは50行以内に収める
- ファイルが多い場合は上位10件に絞る
- git log の全出力をそのまま含めない（要約のみ）
