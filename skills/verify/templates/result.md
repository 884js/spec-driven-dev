# {機能名} — 最終仕様（Result）

> 生成日: {日付}
> 検証モード: {フル検証 / 部分検証}

## 機能概要

{plan.md の概要を元に、実装の実態を1-3文で記述}

## 仕様からの変更点

{差異なし: 「plan.md 通りに実装。変更なし。」}

{差異あり: ユーザーから見た振る舞いが変わるものを変更ごとに以下の形式で記述。振る舞いが同じ内部実装変更は検証メモに記載}

### {振る舞いの変化を1文で。主語は「ユーザー」「システム」「画面」等の機能主体にし、API名・関数名・技術用語を主語にしない}

- **plan.md**: {仕様ではどうなる想定だったか — 機能・振る舞いとして}
- **実装**: {実際にはどう動くか — 機能・振る舞いとして}
- **経緯**: {なぜこの変更が必要になったか。troubleshoot 起因の場合は該当ファイルを参照}
- **技術詳細**: {関連ファイル・関数名など — 必要に応じて}

## ロジック

### 仕様

{この機能が「何をするか」「どう振る舞うか」を箇条書きで記述。実装手順や API 呼び出しではなく、設定値・判定基準・制約・ユーザーに見える結果を書く。技術的な補足は文末に括弧で付記}

### {フロー名}

{Mermaid flowchart でトリガー→判断→結果のフローを図示。主要フローのみ記載（3つ以内を推奨）}

~~~mermaid
flowchart TD
    A[{トリガー/入力}] --> B[{処理/判断}]
    B --> C{{判断基準}}
    C -->|{条件A}| D[{結果A}]
    C -->|{条件B}| E[{結果B}]
~~~

## 受入条件

| # | 受入条件 | 判定 | 備考 |
|---|---------|------|------|
| AC-1 | {受入条件} | {PASS / PARTIAL / FAIL} | {テストファイル or 備考} |
